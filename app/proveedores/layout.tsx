const LayoutProveedores = ({ children }) => {
  return (
    <div className="min-h-full max-w-full">
      <main>
        <div className="sm:px-6 lg:px-8">{children}</div>
      </main>
    </div>
  );
};

export default LayoutProveedores;
